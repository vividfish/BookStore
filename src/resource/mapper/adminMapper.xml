<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.vivid.BookStore.admin.dao.AdminDao">
	<select id="find" resultType="com.vivid.BookStore.admin.domain.Admin">
		select * from t_admin where adminname=#{adminname} and adminpwd=#{dminpwd}
	</select>
</mapper>
